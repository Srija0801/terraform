name: Terraform CI/CD

on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Fake Terraform Init
        run: echo "Terraform initialized successfully"

      - name: Fake Terraform Plan
        run: echo "Terraform plan executed successfully"

      - name: Fake Terraform Apply
        run: echo "Terraform applied successfully" 
